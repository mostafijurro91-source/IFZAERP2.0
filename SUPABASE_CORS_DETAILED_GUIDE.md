# Supabase CORS সেটআপ - বিস্তারিত গাইড

## ধাপ ১: Supabase Dashboard খুলুন
```
URL: https://supabase.com
```
আপনার account দিয়ে login করুন।

---

## ধাপ २: আপনার Project select করুন

Homepage এ আপনি দেখবেন আপনার সব projects:
```
অপশন ১: "vtlsluworsdcluuruneo" project select করুন
অপশন २: যদি homepage এ না থাকে, search করুন
```

Project খোলার পর এই URL দেখবেন:
```
https://supabase.com/dashboard/project/vtlsluworsdcluuruneo
```

---

## ধাপ ३: Project Settings খুলুন

**বাম দিকে Sidebar এ:**

```
├── Home
├── SQL Editor
├── Databases
├── Authentication
├── Storage
├── Vector
├── Realtime
├── Edge Functions
├── Postgres Logs
├── Extensions
├── Database
├── Integrations
└── ⚙️ Settings  ← এখানে ক্লিক করুন
```

**Settings এ ক্লিক করুন** (সবচেয়ে নিচে)

---

## ধাপ ४: API Section খুলুন

Settings খোলার পর বাম দিকে আবার একটি menu দেখবেন:

```
├── General
├── Database
├── API  ← এখানে ক্লিক করুন
├── Authentication
├── Billing
└── Organizations
```

**API** এ ক্লিক করুন।

---

## ধাপ ५: CORS Settings খুজুন

API page এ আপনি এই তথ্য দেখবেন:

```
┌─────────────────────────────────────────┐
│ Project URL                              │
│ https://vtlsluworsdcluuruneo.supabase.co│
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Project API Keys - Anon Public           │
│ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ CORS Configuration                      │
│                                         │
│ Allowed Origins (নিচে স্ক্রল করুন)      │
│                                         │
│ [ইনপুট বক্স]                           │
│ + Add origin                            │
└─────────────────────────────────────────┘
```

**CORS Configuration** section খুজুন।

---

## ধাপ ६: প্রয়োজনীয় URLs যোগ করুন

**Input box এ যোগ করুন একটি করে:**

### প্রথম URL:
```
https://ifzaerp.com
```
Press Enter বা "Add origin" button ক্লিক করুন।

### দ্বিতীয় URL:
```
https://www.ifzaerp.com
```
Press Enter বা "Add origin" button ক্লিক করুন।

### তৃতীয় URL (Development - Optional):
```
http://localhost:5173
```

### চতুর্থ URL (Development - Optional):
```
http://localhost:3000
```

---

## ছবির মতো দেখবে:

```
┌────────────────────────────────────────────┐
│ CORS Configuration                         │
├────────────────────────────────────────────┤
│ Allowed Origins:                           │
│                                            │
│ ✓ https://ifzaerp.com                     │
│ ✓ https://www.ifzaerp.com                 │
│ ✓ http://localhost:5173                   │
│ ✓ http://localhost:3000                   │
│                                            │
│ [ইনপুট বক্স] + Add origin                 │
└────────────────────────────────────────────┘
```

---

## ধাপ ७: Save/Confirm করুন

Page scroll করে নিচে যান এবং:
- ✓ "Save" button থাকলে ক্লিক করুন
- অথবা স্বয়ংক্রিয়ভাবে save হবে (সাধারণত Supabase auto-save করে)

---

## ✅ সম্পন্ন!

এখন আপনার অ্যাপ `https://ifzaerp.com` থেকে Supabase API কল করতে পারবে।

---

## 🔍 যদি CORS Section না খুঁজে পান:

1. **নিশ্চিত করুন আপনি সঠিক project এ আছেন**
   - Dashboard এ যান
   - Project name verify করুন

२. **Page refresh করুন**
   - F5 press করুন
   - কিছুক্ষণ অপেক্ষা করুন

३. **Browser logout এবং login করুন**
   - আবার Supabase login করুন
   - Fresh session এ চেষ্টা করুন

---

## ⚠️ গুরুত্বপূর্ণ মনে রাখবেন:
- Domain যোগ করার ২-३ মিনিট পর কাজ করবে
- যদি এখনও কাজ না করে, একবার আপনার অ্যাপ reload করুন (Ctrl+Shift+R)
